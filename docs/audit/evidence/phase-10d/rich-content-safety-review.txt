Phase 10D rich-content safety review

Stored format:
- PageTranslation.content is a nullable longText field authored through Filament RichEditor.

Verified public renderer:
- No project-approved sanitizer package or public Filament RichEditor renderer is established for Phase 10D.

Chosen strategy:
- App\Services\PublicSite\StaticPageContent::derivePlainText() decodes entities, removes script-like block elements, strips HTML tags, normalizes whitespace, limits text length, and returns plain text.
- StaticPageContent::plainTextParagraphs() prepares escaped paragraph strings for Blade.
- About and Contact Blade output uses {{ }} escaped rendering.

Raw-output search:
rg -n "\{!!" resources/views/public/about.blade.php resources/views/public/contact.blade.php app/Services/PublicSite/StaticPageContent.php

Result:
No matches.

Script-like content behavior:
- script, style, iframe, object, embed, and noscript blocks are removed before strip_tags.

Known limitation:
- Rich formatting is intentionally flattened to plain text until a verified sanitizer or renderer is approved in a later phase.

Verdict:
PASS. Phase 10D does not blindly emit CMS HTML.
